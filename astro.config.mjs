import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://arius0476-hash.github.io',
  base: '/mon-portfolio',  // Assure-toi que le nom ici est EXACTEMENT celui de ton dépôt GitHub
});